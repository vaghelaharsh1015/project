// JavaScript Program to Check if An Array Contains a Specified Value
{
    let fruits = ["apple", "banana", "mango", "orange"];
    let item = "banana";

    if (fruits.includes(item)) {
        console.log(`${item} is found in the array`);
    } else {
        console.log(`${item} is not found in the array`);
    }
}